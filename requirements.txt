numpy>=1.24
scipy>=1.10
torch>=2.1
opencv-python>=4.8
trimesh>=4.0
pyrender>=0.1.45
# HaMeR is installed separately from its repo (git clone + pip install -e .)
# MediaPipe for the independent 2D detector used by reprojection_residual:
mediapipe>=0.10
